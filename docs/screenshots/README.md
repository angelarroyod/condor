# Screenshots

Drop images here and reference them from the top-level `README.md`.

Suggested captures from a running `docker compose up`:

- `terminal.png` — watchlist + live candlestick chart + positions (the hero).
- `options.png` — strategy lab: legs table, payoff diagram, Greeks panel.
- `dashboard.png` — equity curve, allocation donut, risk metric cards.

A short animated GIF of the terminal streaming live prices makes the best hero.
